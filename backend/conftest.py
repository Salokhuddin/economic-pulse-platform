"""
Shared pytest fixtures.

pytest automatically discovers conftest.py files and makes their
fixtures available to all tests in the same directory and below.
We'll add database fixtures, API client helpers, and mock data
factories here as the project grows.
"""
